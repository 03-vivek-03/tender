name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '0 */8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Streamlit App
        run: curl -L https://tendersummarizer.streamlit.app/
